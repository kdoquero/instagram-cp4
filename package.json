{
  "name": "instagram",
  "version": "1.0.0",
  "description": "a instagram app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"nodemon index.js",
    "mysql-start":"sudo docker-compose up",
    "npm run mysql-stop": "sudo docker-compose down",
    "npm run mysql-connect" :"sudo docker-compose exec db mysql -u admin -p",
    "npm run bash": "sudo docker-compose exec db bash"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  }
}
